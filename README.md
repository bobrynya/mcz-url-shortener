# mcz-url-shortener

Production-ready URL shortener на Rust с чистой слоистой архитектурой (Clean Architecture), построенный на axum + sqlx + PostgreSQL.

## 🚀 Возможности

### Основной функционал
- **Сокращение ссылок**: `POST /shorten` принимает одну ссылку или массив ссылок
- **Умная нормализация**: автоматическое приведение URL к канонической форме (lowercase host, удаление фрагментов, дефолтных портов)
- **Дедупликация**: одинаковые URL после нормализации получают один и тот же код
- **Редирект**: `GET /{code}` выполняет 302 редирект на оригинальный URL
- **Асинхронная аналитика**: клики записываются через in-memory очередь и фонового воркера с retry-логикой

### Статистика и аналитика
- **Список ссылок**: `GET /stats` — все ссылки с количеством кликов
- **Детальная статистика**: `GET /stats/{code}` — список всех кликов по конкретной ссылке
- **Пагинация**: параметры `page` и `page_size` (10-50, по умолчанию 25)
- **Фильтрация по датам**: параметры `from` и `to` в формате RFC3339
- **Метаданные кликов**: IP-адрес, User-Agent, Referer, временная метка

### Безопасность и мониторинг
- **Аутентификация**: Bearer token для защиты статистических эндпоинтов
- **Детальные ошибки**: структурированные JSON-ответы с кодами и деталями
- **Access log**: логирование в стиле nginx (IP, метод, путь, статус, latency)
- **Метрики**: счётчики ошибок БД по типам для мониторинга
- **Graceful error handling**: различные типы ошибок БД обрабатываются по-разному

## 🏗️ Архитектура

Проект реализован с использованием **Clean Architecture** (слоистая архитектура) для максимальной поддерживаемости и тестируемости:

```

src/
├── main.rs                    \# Точка входа, композиция зависимостей
├── state.rs                   \# AppState с сервисами
├── error.rs                   \# Обработка ошибок + маппинг sqlx::Error
├── routes.rs                  \# Роутинг HTTP
│
├── api/                       \# 📡 Presentation Layer
│   ├── handlers/              \# HTTP handlers (тонкие, вызывают сервисы)
│   │   ├── shorten.rs
│   │   ├── redirect.rs
│   │   ├── stats.rs
│   │   └── stats_list.rs
│   ├── middleware/            \# HTTP middleware
│   │   ├── access_log.rs
│   │   └── auth.rs
│   └── dto/                   \# Request/Response models
│       ├── shorten.rs
│       ├── stats.rs
│       ├── clicks.rs
│       └── pagination.rs
│
├── application/               \# 💼 Application Layer
│   └── services/              \# Бизнес-логика
│       ├── link_service.rs    \# Создание и управление ссылками
│       ├── stats_service.rs   \# Работа со статистикой
│       └── auth_service.rs    \# Аутентификация
│
├── domain/                    \# 🎯 Domain Layer
│   ├── entities/              \# Доменные сущности
│   │   ├── link.rs
│   │   └── click.rs
│   ├── repositories/          \# Trait-интерфейсы репозиториев
│   │   ├── link_repository.rs
│   │   ├── stats_repository.rs
│   │   └── token_repository.rs
│   ├── click_event.rs         \# Событие клика
│   └── click_worker.rs        \# Воркер обработки кликов
│
├── infrastructure/            \# 🔧 Infrastructure Layer
│   └── persistence/           \# PostgreSQL реализации
│       ├── pg_link_repository.rs
│       ├── pg_stats_repository.rs
│       └── pg_token_repository.rs
│
└── utils/                     \# 🛠️ Утилиты
│   ├── code_generator.rs      \# Генерация коротких кодов
│   └── url_normalizer.rs      \# Нормализация URL

```

### Преимущества архитектуры

✅ **Разделение ответственности**: каждый слой имеет чёткую зону ответственности  
✅ **Тестируемость**: бизнес-логика изолирована от HTTP и БД  
✅ **Независимость от фреймворков**: domain не знает об axum или sqlx  
✅ **Легкая замена инфраструктуры**: можно заменить PostgreSQL на MySQL без изменения бизнес-логики  
✅ **Масштабируемость**: легко добавлять новые фичи и сервисы  

## 📋 Требования

- **Rust**: stable toolchain + cargo
- **PostgreSQL**: 14+ (локально или через Docker)
- **sqlx-cli**: для миграций (опционально)

## ⚙️ Конфигурация

Переменные окружения (или `.env` файл):

```env
# Обязательные
DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/shorty
BASE_URL=https://s.test.com
LISTEN=0.0.0.0:3000

# Логирование
RUST_LOG=info,mcz_url_shortener=debug
```

| Переменная | Описание | Пример |
| :-- | :-- | :-- |
| `DATABASE_URL` | Строка подключения к PostgreSQL | `postgres://user:pass@host/db` |
| `BASE_URL` | Базовый URL для формирования коротких ссылок | `https://s.example.com` |
| `LISTEN` | Адрес и порт для HTTP сервера | `0.0.0.0:3000` |
| `RUST_LOG` | Уровень логирования | `info` / `debug` / `trace` |

## 🚀 Быстрый старт

### 1. Установка sqlx-cli

```bash
cargo install sqlx-cli --no-default-features --features postgres
```


### 2. Создание базы данных и миграции

```bash
# Создать БД
sqlx database create

# Применить миграции
sqlx migrate run
```


### 3. Подготовка offline-режима (опционально)

```bash
# Генерирует .sqlx/ для compile-time проверки SQL без БД
cargo sqlx prepare -- --bin mcz-url-shortener
```


### 4. Запуск сервиса

```bash
# Development режим
cargo run

# Production build
cargo build --release
./target/release/mcz-url-shortener
```


## 📡 API Reference

### Создание коротких ссылок

**Endpoint:** `POST /shorten`

**Content-Type:** `application/json`

**Request Body:**

Вариант 1: Одна ссылка
```json
{
  "url": "https://example.com/very/long/path"
}
```

Вариант 2: Массив ссылок
```json
{
  "urls": [
    "https://example.com",
    "https://github.com"
  ]
}
```

**Response:** `200 OK`

```json
{
  "items": [
    {
      "long_url": "https://example.com/very/long/path",
      "code": "3c1930ac8e",
      "short_url": "https://s.test.com/3c1930ac8e"
    }
  ]
}
```

**Пример с curl:**

```bash
# Одна ссылка
curl -X POST http://127.0.0.1:3000/shorten \
  -H 'Content-Type: application/json' \
  -d '{"url":"https://example.com"}' | jq

# Массив ссылок
curl -X POST http://127.0.0.1:3000/shorten \
  -H 'Content-Type: application/json' \
  -d '{"urls":["https://example.com","https://github.com"]}' | jq
```


---

### Редирект по короткому коду

**Endpoint:** `GET /{code}`

**Response:** `302 Found`

```http
Location: https://example.com/very/long/path
```

**Пример:**

```bash
curl -i http://127.0.0.1:3000/3c1930ac8e
```

**Поведение:**

- Выполняет редирект на оригинальный URL
- Асинхронно записывает событие клика (IP, User-Agent, Referer)

---

### Список всех ссылок со статистикой

**Endpoint:** `GET /stats`

**Authorization:** `Bearer <token>` (обязательно)

**Query Parameters:**


| Параметр | Тип | По умолчанию | Описание |
| :-- | :-- | :-- | :-- |
| `page` | integer | 1 | Номер страницы (начиная с 1) |
| `page_size` | integer | 25 | Размер страницы (10-50) |
| `from` | RFC3339 | - | Фильтр: клики от даты |
| `to` | RFC3339 | - | Фильтр: клики до даты |

**Response:** `200 OK`

```json
{
  "items": [
    {
      "code": "3c1930ac8e",
      "long_url": "https://example.com",
      "total_clicks": 42,
      "created_at": "2026-01-16T10:30:00Z"
    }
  ],
  "pagination": {
    "page": 1,
    "page_size": 25,
    "total_items": 157,
    "total_pages": 7
  }
}
```

**Пример:**

```bash
curl "http://127.0.0.1:3000/stats?page=1&page_size=10" \
  -H "Authorization: Bearer YOUR_TOKEN" | jq
```


---

### Детальная статистика по коду

**Endpoint:** `GET /stats/{code}`

**Authorization:** `Bearer <token>` (обязательно)

**Query Parameters:** те же что и у `/stats`

**Response:** `200 OK`

```json
{
  "code": "3c1930ac8e",
  "long_url": "https://example.com",
  "created_at": "2026-01-16T10:30:00Z",
  "total_clicks": 42,
  "recent_clicks": [
    {
      "clicked_at": "2026-01-16T18:45:23Z",
      "user_agent": "Mozilla/5.0...",
      "referer": "https://news.ycombinator.com/",
      "ip": "203.0.113.42"
    }
  ],
  "pagination": {
    "page": 1,
    "page_size": 25,
    "total_items": 42,
    "total_pages": 2
  }
}
```

**Пример с фильтрацией:**

```bash
curl "http://127.0.0.1:3000/stats/3c1930ac8e?from=2026-01-01T00:00:00Z&to=2026-01-16T23:59:59Z" \
  -H "Authorization: Bearer YOUR_TOKEN" | jq
```


---

## 🔐 Аутентификация

Защищённые эндпоинты: `GET /stats`, `GET /stats/{code}`

**Формат заголовка:**

```http
Authorization: Bearer <your-token>
```

**Создание токена** (выполните SQL напрямую в БД):

```sql
-- Вставить токен (хранится SHA256 хеш)
INSERT INTO api_tokens (name, token_hash)
VALUES ('My App', encode(sha256('your-secret-token'::bytea), 'hex'));
```

**Использование:**

```bash
export TOKEN="your-secret-token"
curl -H "Authorization: Bearer $TOKEN" http://127.0.0.1:3000/stats
```


---

## ❌ Обработка ошибок

Все ошибки возвращаются в едином JSON-формате:

```json
{
  "error": {
    "code": "validation_error",
    "message": "Invalid URL format",
    "details": {
      "reason": "Only HTTP and HTTPS protocols are allowed"
    }
  }
}
```

**Типы ошибок:**


| HTTP Status | Error Code | Описание |
| :-- | :-- | :-- |
| 400 | `validation_error` | Некорректные входные данные |
| 401 | `unauthorized` | Отсутствует или неверный токен |
| 404 | `not_found` | Ресурс не найден |
| 409 | `conflict` | Конфликт (например, дубликат) |
| 500 | `internal_error` | Внутренняя ошибка сервера |

**Примеры:**

Неверный URL
```json
{
  "error": {
    "code": "validation_error",
    "message": "Invalid URL format",
    "details": { "reason": "Invalid URL: relative URL without a base" }
  }
}
```
Код не найден
```json
{
  "error": {
    "code": "not_found",
    "message": "Short link not found",
    "details": { "code": "unknown123" }
  }
}
```

Неавторизован
```json
{
  "error": {
    "code": "unauthorized",
    "message": "Unauthorized",
    "details": { "reason": "Invalid or revoked token" }
  }
}
```
---

## 📊 Мониторинг и логи

### Логирование

Используется `tracing` для структурированных логов:

```bash
# Только важные события
RUST_LOG=info cargo run

# Детальная отладка
RUST_LOG=debug,mcz_url_shortener=trace cargo run
```


### Метрики

Встроенные счётчики (через `metrics` crate):

- `click_worker_received_total` — события кликов получено
- `click_worker_processed_total` — события успешно обработано
- `click_worker_failed_total` — ошибки обработки
- `click_worker_retried_total` — количество retry
- `database_errors_total{type="..."}` — ошибки БД по типам
---

## 📝 Лицензия

MIT

---

## 🤝 Contributing

Pull requests приветствуются! Для крупных изменений сначала откройте issue для обсуждения.

---

## 🔗 Дополнительные ресурсы

- [Axum Documentation](https://docs.rs/axum)
- [SQLx Documentation](https://docs.rs/sqlx)
- [Clean Architecture by Robert Martin](https://blog.cleancoder.com/uncle-bob/2012/08/13/the-clean-architecture.html)
